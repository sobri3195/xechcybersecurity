import { Link,useParams } from 'react-router-dom'; import Seo from '../components/Seo'; import Breadcrumb from '../components/Breadcrumb'; import OfficialPlaceholder from '../components/officials/OfficialPlaceholder'; import { officials } from '../data/officials';
export default function OfficialDetail(){const {slug}=useParams(),x=officials.find(o=>o.slug===slug);if(!x)return <div className="container-site section pt-32"><Breadcrumb items={[{label:'Pejabat',to:'/officials'},{label:'Tidak ditemukan'}]}/><h1 className="heading">Profil pejabat tidak ditemukan</h1></div>;return <><Seo title={`${x.name} — ${x.position} | Xech Cyber Security`} description={x.biography} path={`/officials/${slug}`}/><article className="container-site section pt-32"><Breadcrumb items={[{label:'Profil Pejabat',to:'/officials'},{label:x.name}]}/><div className="grid gap-8 md:grid-cols-[280px_1fr]"><div><OfficialPlaceholder alt={x.imageAlt}/>{x.isPlaceholder&&<span className="badge mt-4">Data Demonstrasi</span>}</div><div><h1 className="heading">{x.name} {x.degrees}</h1><p className="mt-3 text-lg">{x.position} · {x.unit}</p><p className="text-muted">{x.periodStart}–{x.periodEnd||'sekarang'} · {x.status}</p><h2 className="mt-8 text-xl font-bold">Biografi resmi</h2><p className="text-muted">{x.biography}</p>{[['Riwayat pendidikan',x.education],['Riwayat jabatan',x.careerHistory],['Bidang tanggung jawab',x.responsibilities],['Tautan resmi',x.officialLinks]].map(([h,a])=><section className="mt-6" key={h}><h2 className="text-xl font-bold">{h}</h2><p className="text-muted">{a.length?a.join(', '):'Data resmi belum tersedia.'}</p></section>)}<p className="mt-6 text-sm">Terakhir diperbarui: {x.lastUpdated}</p><Link className="btn-secondary mt-8" to="/officials">← Kembali ke direktori</Link></div></div></article></>}
