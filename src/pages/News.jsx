import { useMemo } from 'react'; import { useSearchParams } from 'react-router-dom'; import PageHero from '../components/PageHero'; import Seo from '../components/Seo'; import FeaturedNews from '../components/news/FeaturedNews'; import NewsCard from '../components/news/NewsCard'; import NewsFilters from '../components/news/NewsFilters'; import { news } from '../data/news'; import { matchesSearch } from '../utils/searchFilter';
export default function News(){const [params,setParams]=useSearchParams();const value={search:params.get('search')||'',category:params.get('category')||'',year:params.get('year')||'',sort:params.get('sort')||'new'};const setValue=(k,v)=>{const n=new URLSearchParams(params);v?n.set(k,v):n.delete(k);setParams(n)};const filtered=useMemo(()=>news.filter(x=>matchesSearch(x,value.search,['title','excerpt','category'])&&(!value.category||x.category===value.category)&&(!value.year||x.publishedAt.startsWith(value.year))).sort((a,b)=>(value.sort==='old'?1:-1)*a.publishedAt.localeCompare(b.publishedAt)),[value.search,value.category,value.year,value.sort]);return <><Seo title="Berita Keamanan Siber | Xech Cyber Security" description="Berita, kegiatan, dan perkembangan terbaru dari Xech Cyber Security." path="/news"/><PageHero eyebrow="PUBLIKASI" title="Berita Keamanan Siber" description="Perkembangan terbaru, kegiatan, dan kabar penting dari Xech Cyber Security."/><div className="container-site section"><FeaturedNews item={news.find(x=>x.featured)}/><div className="mt-12"><NewsFilters value={value} setValue={setValue} categories={[...new Set(news.map(x=>x.category))]} years={[...new Set(news.map(x=>x.publishedAt.slice(0,4)))]} count={filtered.length} onReset={()=>setParams({})}/><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{filtered.map(x=><NewsCard key={x.id} item={x}/>)}</div>{!filtered.length&&<div className="empty">Tidak ada berita yang cocok. Gunakan Reset filter.</div>}</div></div></>}
