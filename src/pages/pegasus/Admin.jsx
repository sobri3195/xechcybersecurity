import {useState} from 'react'; import {demoChallenges} from './catalog';
export default function Admin(){const [tab,setTab]=useState('Challenges');return <section className="peg-wrap peg-page"><p className="peg-kicker">ROLE: ADMINISTRATOR</p><h1>PEGASUS Control Room</h1><div className="peg-tabs">{['Challenges','Submission','Statistik','Import / Export','Audit Log'].map(x=><button key={x} aria-pressed={tab===x} onClick={()=>setTab(x)}>{x}</button>)}</div><div className="peg-adminbar"><button className="peg-button">+ Challenge Baru</button><button className="peg-button ghost">Impor JSON / CSV</button><button className="peg-button ghost">Ekspor</button></div><div className="peg-panel"><div className="peg-table admin"><div className="head"><span>ID</span><span>JUDUL</span><span>LEVEL</span><span>POIN</span><span>STATUS</span><span>AKSI</span></div>{demoChallenges.slice(0,8).map(x=><div key={x.id}><code>PG-{String(x.id).padStart(3,'0')}</code><strong>{x.title}</strong><span>{x.difficulty}</span><span>{x.points}</span><span className="ok">Aktif</span><span><button aria-label={`Ubah ${x.title}`}>✎</button> <button aria-label={`Duplikasi ${x.title}`}>⧉</button> <button aria-label={`Hapus ${x.title}`}>⌫</button></span></div>)}</div></div><p className="peg-admin-note">Perubahan flag hanya menerima nilai mentah melalui koneksi TLS, langsung di-hash server, dan tidak pernah dikirim kembali. Reset skor memerlukan alasan dan menghasilkan audit log immutable.</p></section>}
