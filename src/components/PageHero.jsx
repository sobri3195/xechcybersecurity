import { Link } from 'react-router-dom'; import { ChevronRight } from 'lucide-react'; import { motion,useReducedMotion } from 'framer-motion'
export default function PageHero({current,title,description}){const reduce=useReducedMotion();return <section className="grid-bg border-b border-white/[.08] pb-16 pt-32"><motion.div initial={reduce?false:{opacity:0,y:16}} animate={{opacity:1,y:0}} className="container-site"><nav aria-label="Breadcrumb" className="mb-6 flex items-center gap-2 text-sm text-muted"><Link to="/" className="hover:text-neon">Home</Link><ChevronRight size={14} aria-hidden="true"/><span aria-current="page" className="text-copy">{current}</span></nav><h1 className="hero-title max-w-4xl">{title}</h1>{description&&<p className="mt-5 max-w-2xl text-lg text-muted">{description}</p>}</motion.div></section>}
