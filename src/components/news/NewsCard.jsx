import { Link } from 'react-router-dom'; export default function NewsCard({item}){return <article className="card flex flex-col"><img src={item.image} alt={item.imageAlt} className="mb-5 aspect-video w-full rounded-xl bg-white/5 object-cover"/><p className="eyebrow">{item.category} · Data Demonstrasi</p><h3 className="mt-3 font-mono text-xl font-bold">{item.title}</h3><p className="mt-3 flex-1 text-muted">{item.excerpt}</p><p className="mt-4 text-xs text-muted">{item.publishedAt} · {item.readTime} menit · {item.author}</p><Link className="mt-4 text-neon" to={`/news/${item.slug}`}>Baca Selengkapnya →</Link></article>}
