import { Link } from 'react-router-dom'; import { ArrowUpRight } from 'lucide-react'; import { motion,useReducedMotion } from 'framer-motion'
export default function ServiceCard({service}){const Icon=service.icon,reduce=useReducedMotion();return <motion.article whileHover={reduce?{}:{y:-5}} className="card group flex h-full flex-col"><div className="mb-5 grid h-12 w-12 place-items-center rounded-xl border border-neon/30 bg-neon/10 text-neon"><Icon aria-hidden="true"/></div><h3 className="font-mono text-xl font-semibold text-copy">{service.title}</h3><p className="mt-3 flex-1 text-sm leading-7 text-muted">{service.description}</p><Link to={`/services#${service.slug}`} className="mt-6 inline-flex min-h-11 items-center gap-2 font-mono text-sm text-copy outline-none group-hover:text-neon focus-visible:text-neon">Selengkapnya <ArrowUpRight size={16} aria-hidden="true"/></Link></motion.article>}
