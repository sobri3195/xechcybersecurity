import { useState } from 'react'; import { storage } from '../../utils/storage';
export default function InteractiveChecklist({slug,items}){const key=`xech_information_checklist_${slug}`;const [done,setDone]=useState(()=>storage.get(key,[]));const toggle=i=>{const n=done.includes(i)?done.filter(x=>x!==i):[...done,i];setDone(n);storage.set(key,n)};return <section className="card my-8"><h2 className="text-xl font-bold">Checklist interaktif</h2>{items.map((x,i)=><label className="mt-3 flex gap-3" key={x}><input type="checkbox" checked={done.includes(i)} onChange={()=>toggle(i)}/>{x}</label>)}<button className="btn-secondary mt-5" onClick={()=>{setDone([]);storage.remove(key)}}>Reset checklist</button><p className="mt-3 text-xs text-muted">Progres hanya tersimpan pada perangkat ini, tidak memuat data sensitif, dan bukan audit profesional.</p></section>}
