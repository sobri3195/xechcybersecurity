import { Link } from 'react-router-dom'; export default function InformationCard({item}){return <article className="card"><p className="eyebrow">{item.topic} · {item.contentType}</p><h3 className="mt-3 font-mono text-xl font-bold">{item.title}</h3><p className="mt-3 text-muted">{item.excerpt}</p><p className="mt-4 text-sm">{item.level} · {item.readTime} menit</p><Link className="mt-4 inline-block text-neon" to={`/information/${item.slug}`}>Baca panduan →</Link></article>}
