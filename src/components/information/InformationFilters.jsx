export default function InformationFilters({value,setValue,items,count,onReset}){const opts=k=>[...new Set(items.map(x=>x[k]))];return <div className="filterbar"><label>Cari<input value={value.search} onChange={e=>setValue('search',e.target.value)}/></label>{[['topic','Topik'],['level','Tingkat'],['contentType','Tipe']].map(([k,l])=><label key={k}>{l}<select value={value[k]} onChange={e=>setValue(k,e.target.value)}><option value="">Semua</option>{opts(k).map(x=><option key={x}>{x}</option>)}</select></label>)}<button className="btn-secondary" onClick={onReset}>Reset filter</button><p aria-live="polite">{count} hasil</p></div>}
