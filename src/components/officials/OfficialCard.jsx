import { Link } from 'react-router-dom'; import OfficialPlaceholder from './OfficialPlaceholder'; export default function OfficialCard({item}){return <article className="card"><OfficialPlaceholder alt={item.imageAlt}/>{item.isPlaceholder&&<span className="badge mt-4">Data Demonstrasi</span>}<h3 className="mt-3 font-mono text-xl font-bold">{item.name}</h3><p>{item.degrees} {item.position}</p><p className="text-muted">{item.unit} · {item.periodStart} · {item.status}</p><Link className="mt-4 inline-block text-neon" to={`/officials/${item.slug}`}>Lihat Profil →</Link></article>}
