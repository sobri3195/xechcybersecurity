export default function QuizStats({stats}){return <div className="grid grid-cols-2 gap-3 sm:grid-cols-5">{[['Streak',stats.currentStreak],['Best streak',stats.bestStreak],['Skor terbaik',`${stats.bestScore}/5`],['Total',stats.total],['Rata-rata',`${stats.average.toFixed(1)}/5`]].map(x=><div className="card p-4" key={x[0]}><strong className="text-xl text-neon">{x[1]}</strong><p className="text-xs text-muted">{x[0]}</p></div>)}</div>}
