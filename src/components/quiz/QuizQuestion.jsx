export default function QuizQuestion({item,value,onChange}){return <fieldset className="mt-7"><legend className="text-xl font-bold">{item.question}</legend><p className="my-3 text-sm text-muted">{item.category} · {item.difficulty}</p><div className="grid gap-3">{item.options.map((x,i)=><label className={`option ${value===i?'selected':''}`} key={x}><input type="radio" name={item.id} checked={value===i} onChange={()=>onChange(i)}/>{x}</label>)}</div></fieldset>}
