import { useState } from 'react'; import { motion,AnimatePresence,useReducedMotion } from 'framer-motion'; import { NavLink,Link } from 'react-router-dom';
export default function MobileMenu({open,onClose}){const reduce=useReducedMotion(),[pub,setPub]=useState(false);return <AnimatePresence>{open&&<motion.div initial={reduce?false:{height:0}} animate={{height:'auto'}} exit={{height:0}} className="overflow-hidden border-t border-white/10 bg-ink/95 md:hidden"><nav className="container-site flex flex-col py-4" aria-label="Navigasi mobile">{[['/','Home'],['/about','About'],['/services','Services'],['/articles','Articles']].map(x=><NavLink className="mobile-link" key={x[0]} to={x[0]} onClick={onClose}>{x[1]}</NavLink>)}<button className="mobile-link text-left" aria-expanded={pub} aria-controls="mobile-publication" onClick={()=>setPub(v=>!v)}>Publikasi</button>{pub&&<div id="mobile-publication" className="pl-5">{[['/news','Berita'],['/information','Informasi'],['/officials','Profil Pejabat']].map(x=><NavLink className="mobile-link" key={x[0]} to={x[0]} onClick={onClose}>{x[1]}</NavLink>)}</div>}<NavLink className="mobile-link" to="/quiz" onClick={onClose}>Quiz</NavLink><NavLink className="mobile-link" to="/contact" onClick={onClose}>Contact</NavLink><Link to="/contact" onClick={onClose} className="btn-primary mt-4">Konsultasi</Link></nav></motion.div>}</AnimatePresence>}
