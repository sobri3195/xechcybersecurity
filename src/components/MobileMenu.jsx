import { useEffect, useRef } from 'react'
import { NavLink, Link } from 'react-router-dom'
const links=[['/','Home'],['/about','About'],['/services','Services'],['/articles','Insight'],['/journal','Journal'],['/news','Berita'],['/information','Informasi'],['/officials','Profil Pejabat'],['/pegasus','PEGASUS CTF'],['/quiz','Quiz'],['/contact','Contact']]
export default function MobileMenu({open,onClose}){const panel=useRef(null);useEffect(()=>{if(!open)return;const previous=document.activeElement;document.body.style.overflow='hidden';panel.current?.querySelector('a')?.focus();const key=e=>{if(e.key==='Escape')onClose();if(e.key==='Tab'){const items=[...panel.current.querySelectorAll('a,button')],first=items[0],last=items.at(-1);if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}};addEventListener('keydown',key);return()=>{document.body.style.overflow='';removeEventListener('keydown',key);previous?.focus()}},[open,onClose]);if(!open)return null;return <div className="mobile-menu-layer lg:hidden"><button className="mobile-backdrop" aria-label="Tutup menu" onClick={onClose}/><div className="mobile-menu-panel" ref={panel}><nav aria-label="Navigasi mobile">{links.map(([to,label])=><NavLink end={to==='/'} className="mobile-link" key={to} to={to} onClick={onClose}>{label}</NavLink>)}<Link to="/contact" onClick={onClose} className="btn-primary mt-4">Konsultasi <span aria-hidden="true">→</span></Link></nav></div></div>}
