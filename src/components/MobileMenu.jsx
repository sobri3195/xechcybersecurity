import { motion, AnimatePresence, useReducedMotion } from 'framer-motion'; import { NavLink, Link } from 'react-router-dom'
const links=[['/','Home'],['/about','About'],['/services','Services'],['/contact','Contact']]
export default function MobileMenu({open,onClose}){const reduce=useReducedMotion();return <AnimatePresence>{open&&<motion.div initial={reduce?false:{height:0,opacity:0}} animate={{height:'auto',opacity:1}} exit={{height:0,opacity:0}} className="overflow-hidden border-t border-white/10 bg-ink/95 md:hidden"><nav aria-label="Navigasi mobile" className="container-site flex flex-col py-4">{links.map(([to,label])=><NavLink key={to} end={to==='/'} to={to} onClick={onClose} className={({isActive})=>`flex min-h-12 items-center border-b border-white/5 px-2 font-mono ${isActive?'text-neon':'text-copy'}`}>{label}</NavLink>)}<Link to="/contact" onClick={onClose} className="btn-primary mt-4">Konsultasi</Link></nav></motion.div>}</AnimatePresence>}
